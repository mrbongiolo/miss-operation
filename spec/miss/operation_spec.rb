require 'spec_helper'

describe Miss::Operation do
  it 'has a version number' do
    expect(Miss::Operation::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
